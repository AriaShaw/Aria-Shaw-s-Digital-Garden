---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <!-- Homepage -->
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>

  <!-- Blog Posts - Highest Priority Content -->
  {% for post in site.posts %}
  <url>
    <loc>{{ site.url }}{{ post.url }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
  </url>
  {% endfor %}

  <!-- Static Pages -->
  {% for page in site.pages %}
    {% unless page.url contains '/resources/' or page.url contains '/assets/' or page.url contains '/templates/' or page.url contains 'google' or page.url contains '.png' or page.url contains '.ico' or page.url contains 'sitemap.xml' or page.url contains 'robots.txt' or page.url contains '404' or page.url contains 'feed.xml' %}
      {% if page.url != '/' %}
  <url>
    <loc>{{ site.url }}{{ page.url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>{% if page.url contains '/toolkit/' or page.url contains '/scripts/' %}weekly{% else %}monthly{% endif %}</changefreq>
    <priority>{% if page.url contains '/toolkit/' %}0.8{% elsif page.url contains '/scripts/' %}0.8{% elsif page.url contains '/about/' or page.url contains '/support/' %}0.7{% else %}0.5{% endif %}</priority>
  </url>
      {% endif %}
    {% endunless %}
  {% endfor %}

</urlset>