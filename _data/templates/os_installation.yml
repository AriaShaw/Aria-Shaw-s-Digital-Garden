# Template: OS Installation Guide
# Template ID: os_installation
# Intent Type: practitioner
# Variables: {{os_name}}, {{os_version}}, {{odoo_version}}

metadata:
  title: "Install Odoo {{odoo_version}} on {{os_name}} {{os_version}}: Complete Guide"
  description: "Step-by-step installation of Odoo {{odoo_version}} on {{os_name}} {{os_version}}. Includes PostgreSQL setup, dependencies, and systemd service configuration."
  intent_type: "practitioner"
  page_slug: "install-odoo-{{odoo_version}}-on-{{os_slug}}"

static_data:
  os_name: "{{os_name}}"
  os_version: "{{os_version}}"
  odoo_version: "{{odoo_version}}"
  installation_method: "from_source"
  
  installation_steps:
    - "Update system packages"
    - "Install PostgreSQL"
    - "Install Python 3.10+ and dependencies"
    - "Create odoo system user"
    - "Clone Odoo from GitHub"
    - "Install Python requirements"
    - "Configure odoo.conf"
    - "Create systemd service"
    - "Start and enable Odoo"
    
ai_generated_content:
  introduction: ""
  installation_guide: ""
  troubleshooting: ""
  
conversion_assets:
  - component_type: "callout-box"
    generated_copy: {}
    
intelligent_links:
  - link_type: "related_deployment"
    generated_link: {}
