# Template: Data Migration Guide
# Usage: migrate-from-{{source_system}}-to-odoo

metadata:
  title: "Migrate from {{source_system}} to Odoo {{odoo_version}}: Complete 2025 Guide"
  description: "Step-by-step guide to migrate your data from {{source_system}} to Odoo {{odoo_version}}. Includes field mapping, data validation, and rollback strategies."
  intent_type: "practitioner"
  page_slug: "migrate-from-{{source_slug}}-to-odoo"
  
static_data:
  source_system: "{{source_system}}"
  target_system: "Odoo {{odoo_version}}"
  migration_complexity: "{{complexity}}"
  
  migration_phases:
    - phase: "Assessment & Planning"
      duration: "1-2 weeks"
      tasks:
        - "Audit {{source_system}} data quality"
        - "Map {{source_system}} fields to Odoo models"
        - "Identify custom fields and workflows"
        - "Calculate migration timeline and resources"
        
    - phase: "Data Preparation"
      duration: "2-4 weeks"
      tasks:
        - "Export data from {{source_system}}"
        - "Clean and deduplicate records"
        - "Transform data to Odoo format"
        - "Validate data integrity"
        
    - phase: "Test Migration"
      duration: "1-2 weeks"
      tasks:
        - "Set up Odoo staging environment"
        - "Import sample dataset"
        - "Test business processes"
        - "Train key users"
        
    - phase: "Production Migration"
      duration: "1-3 days"
      tasks:
        - "Schedule downtime window"
        - "Final data export from {{source_system}}"
        - "Import to Odoo production"
        - "Verify data completeness"
        - "Go-live validation"
        
  key_considerations:
    - "{{source_system}} data export formats: {{export_formats}}"
    - "Field mapping complexity: {{mapping_notes}}"
    - "Expected data volume: {{data_volume}}"
    - "Recommended migration tool: {{tool_recommendation}}"
    
  common_challenges:
    - challenge: "Data format incompatibility"
      solution: "Use Odoo's import templates and CSV format converters"
    - challenge: "Custom field migration"
      solution: "Create custom Odoo fields or use Studio module"
    - challenge: "Historical data preservation"
      solution: "Import with original dates using import_batch_size parameter"
      
ai_generated_content:
  introduction: ""
  migration_guide: ""
  
conversion_assets:
  - component_type: "callout-box"
    generated_copy: {}
