{
  "buildCommand": "JEKYLL_ENV=production bundle exec jekyll build",
  "outputDirectory": "_site",
  "installCommand": "gem install bundler:2.3.26 && bundle install",
  "devCommand": "bundle exec jekyll serve --host 0.0.0.0 --port $PORT",
  "cleanUrls": true,
  "trailingSlash": false
}
