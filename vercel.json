{
  "buildCommand": "bundle install && JEKYLL_ENV=production bundle exec jekyll build",
  "outputDirectory": "_site",
  "framework": null,
  "installCommand": "gem install bundler && bundle install",
  "devCommand": "bundle exec jekyll serve --host 0.0.0.0 --port $PORT"
}
